# Copy to .env for local compose overrides (do not commit secrets).
POSTGRES_PASSWORD=change-me
KEYCLOAK_ADMIN_PASSWORD=change-me
KEYCLOAK_ADMIN_CLIENT_SECRET=shortener-admin-secret
IP_HASH_PEPPER=change-me-long-random
# Optional: host path containing GeoLite2-Country.mmdb
# GEO_MAXMIND_HOST_PATH=./deploy/geoip
